# output/

What comes off the table. Drafts, rewrites, reviews — the thing you actually
asked for.

Claude Code writes here. The advisors cannot: they run read-only and their
opinions land in `table/<job>/notes/` instead.

The contents are gitignored.
